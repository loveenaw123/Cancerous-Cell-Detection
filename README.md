# Cancerous-Cell-Detection
Detects whether a given cell is malignant or benign using the set of features like cell_radius,compactness_mean,concavity_mean.Various machine learning algorithms such as SVM,Naive Bayes and Logistic Regression are used and their accuracies are compared .
